{
  "name": "accessible-resume-builder",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "npm run build && npx http-server . -p 8080",
    "dev": "npm run build:watch & npx http-server . -p 8080",
    "clean": "rm -rf dist/*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}